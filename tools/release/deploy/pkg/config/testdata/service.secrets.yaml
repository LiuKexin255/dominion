name: service
app: grpc-hello-world
desc: grpc hello world service with secrets
artifacts:
  - name: service
    target: :service_image
    secrets:
      - database-url
      - stripe-api-key
    ports: 
      - name: grpc 
        port: 50051
