version: "3.0"
name: service
app: grpc-hello-world
desc: grpc hello world service with invalid json config
configs:
  - name: service_config
    data:
      - name: limits
        value: '{"maxConn": }'
        type: json
artifacts:
  - name: service
    target: :service_image
    ports:
      - name: grpc
        port: 50051
