name: grpc.dev
desc: "开发环境"
type: dev
services:
  - artifact: 
      path: service/service.yaml
      name: service
      secrets:
        database-url:
          secret: prod-db
          key: username
