name: grpc.dev
desc: "开发环境"
type: dev
services:
  - artifact: 
      path: service/service.yaml
      name: service
  - artifact: 
      path: gateway/service.yaml
      name: gateway 
    http: 
      hostnames:
        - hello.liukexin.com
      matches:
        - backend: http
          path: 
            type: PathPrefix
            value: /v1
