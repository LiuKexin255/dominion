name: service-with-secrets
app: alpha
desc: service with secrets
artifacts:
  - name: service-with-secrets
    target: :service_with_secrets_image
    tls: true
    secrets:
      - db-password
      - api-key
    ports:
      - name: grpc
        port: 50051
      - name: http
        port: 8080
