name: grpc.dev
desc: "开发环境"
services:
  - infra:
      resource: mongodb
      profile: development
      name: grpc-hello-world-mongo
      app: grpc-hello-world
      persistence:
        enabled: true
