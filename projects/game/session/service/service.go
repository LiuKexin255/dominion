// Package service provides the session business logic layer.
//
// The session service is simple enough that the handler delegates directly
// to the domain repository. This package is reserved for future logic
// that does not belong in the handler or domain layers.
package service
