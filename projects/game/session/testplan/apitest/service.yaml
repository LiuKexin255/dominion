version: "3.0"
name: session-apitest
app: game
desc: game session test-only HTTP api shell
kind: stateless
artifacts:
  - name: cmd
    target: :cmd_image
    tls: true
    ports:
      - name: http
        port: 8080
