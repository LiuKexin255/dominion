name: game.glt
desc: "game-gateway interface large test"
type: test
services:
  - artifact:
      path: //projects/game/gateway/service.yaml
      name: cmd
    http:
      hostnames:
        - game.liukexin.com
      matches:
        - backend: http
          path:
            type: PathPrefix
            value: /
