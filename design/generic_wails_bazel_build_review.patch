diff --git a/design/generic_wails_bazel_build.md b/design/generic_wails_bazel_build.md
index 0000000..0000000 100644
--- a/design/generic_wails_bazel_build.md
+++ b/design/generic_wails_bazel_build.md
@@
 ## 当前问题
 
-当前仓库中 Wails 构建链路存在以下问题：
+当前仓库中 Wails 构建链路存在以下问题。需要特别注意：
+`projects/game/windows_agent` 当前无法构建的第一失败点并不在 Wails Go runtime、
+frontend embed 或 zip package 阶段，而是在 Bazel analysis 阶段的 Windows target
+toolchain resolution。
 
 1. **`wails_app` 不是最终构建入口**：`projects/game/windows_agent:windows_agent_app` 只聚合部分 Wails 阶段输出，`WailsAppInfo.binary` 为空；最终 zip 仍直接依赖 `cmd/windows_agent:windows_agent_windows`。
 2. **Wails 构建语义分散在项目侧**：Windows `go_binary` 的 `goos/goarch`、Wails production tags、linkopts、resource `.syso` 接入没有由 `tools/wails` 统一封装。
 3. **本地化 Wails 源码维护成本高**：当前 `//third_party/github.com/wailsapp/wails/v2` 同时承担 Wails CLI 来源和 runtime 依赖来源。若不需要 patch Wails 源码或从源码构建 CLI，则本地化会增加 BUILD 文件维护和 Gazelle 重定向复杂度。
 4. **Wails CLI 职责容易混淆**：CLI 对 bindings、diagnostics 有价值，但 `wails build` 是面向工作区的黑盒编排命令，不适合作为 Bazel 生产 action。
-5. **Windows 目标平台与 CGo 认知混乱**：Wails v2 Windows runtime 本身不需要 CGo；当前目标平台仅 Windows，因此不应为了 Linux/macOS Wails CGo 场景引入 Windows C++ cross toolchain 作为默认要求。
+5. **Windows 目标平台与 CGo / launcher toolchain 认知混乱**：Wails v2 Windows runtime 本身不需要 CGo；但 `go_binary(goos = "windows", goarch = "amd64")` 在 Bazel/rules_go 下仍可能触发 Bazel Windows launcher 相关的 `@@bazel_tools//tools/cpp:toolchain_type` 解析。`pure = "on"` 只能避免 Go/CGo 编译路径，不能替代 Bazel 所需的 Windows target C++ launcher/toolchain 能力。
+6. **当前实测失败为 Windows target C++ toolchain 缺失**：`bazel build //projects/game/windows_agent:windows_agent_win_zip`、`//projects/game/windows_agent:windows_agent_app_binary` 和 legacy `//projects/game/windows_agent/cmd/windows_agent:windows_agent_windows` 均在 `@@bazel_tools//src/tools/launcher:launcher` 解析 C++ toolchain 时失败。Linux host C++ toolchain 可用于 `@@platforms//host:host`，但会被拒绝用于 `@@rules_go+//go/toolchain:windows_amd64`。
+7. **当前实现状态与原问题描述已变化**：当前 `windows_agent_win_zip.binary` 已经指向 `:windows_agent_app`，不再直接依赖 `cmd/windows_agent:windows_agent_windows`；因此后续 review 应以当前 BUILD 图为准，不应继续把“zip 绕过 `wails_app`”作为当前失败根因。
@@
 ### Windows 平台构建语义
 
 当前支持的生产平台：`windows/amd64`。
 
+在设计 Windows 构建前必须先区分三类 C/C++/toolchain 需求：
+
+1. **Wails runtime CGo 需求**：Wails v2 Windows runtime 当前可按 pure Go 路径构建，不要求 CGo。
+2. **Bazel/rules_go Windows target launcher 需求**：即使 `pure = "on"`，Bazel 在构建 Windows `go_binary` 时仍可能为 Windows launcher 解析 `@@bazel_tools//tools/cpp:toolchain_type`；这需要一个匹配 Windows amd64 target platform 的 C++ toolchain 或等价 launcher 策略。
+3. **项目自身 CGo/C/C++ 需求**：只有业务代码或依赖实际引入 `import "C"`、C/C++ 源码或 C deps 时，才需要完整 Windows CGo cross toolchain，并应显式 `pure = "off"`。
+
+因此，本方案的 Windows production binary 默认仍应使用 pure Go，但迁移步骤必须先验证 Bazel 能分析并构建一个最小 Windows `go_binary`，不能只依赖 `pure = "on"`。
+
 `wails_app(platform = "windows/amd64")` 内部生成或包装：
 
 ```starlark
 go_binary(
@@
     gotags = [
         "desktop",
         "production",
         "wv2runtime.embed",
-        "native_webview2loader",
     ],
@@
 )
 ```
 
 说明：
 
-* `pure = "on"` 是 Windows-only 当前目标下的默认选择。Wails v2 Windows 端不需要 CGo，避免引入 Windows C++ cross toolchain。
+* `pure = "on"` 是 Windows-only 当前目标下的默认选择。Wails v2 Windows 端不需要 CGo，避免为 Wails runtime 本身引入 Windows CGo cross toolchain。但它不保证 Bazel Windows launcher 不需要 target C++ toolchain。
 * `webview2` 暴露为属性，可取 `download`、`embed`、`browser`、`error`，映射为 `wv2runtime.<value>` build tag。
-* `native_webview2loader` 作为当前 Wails v2 Windows 默认 loader tag 保留。
+* 默认不添加 `native_webview2loader`。当前 upstream 证据表明该 tag 更接近 legacy native DLL loader 选择项；若项目确实需要该路径，应通过显式属性启用，并用 Windows smoke test 验证。
 * 如果未来某项目自身引入 `import "C"`，该项目不能使用默认 pure Windows 路径，应单独设计 CGo Windows toolchain 支持。
+* `gc_linkopts = ["-w", "-s", "-H", "windowsgui"]` 应保留，并用 fixture 验证最终二进制为 Windows GUI subsystem。
+* `out` 设置时 rules_go 不会自动追加 `.exe`，因此 `out = binary_name + ".exe"` 是必需的。
@@
 ### Frontend assets 模型
 
 通用方案不规定具体前端框架，只要求 `frontend` 属性指向一个 Bazel target，该 target 输出前端 dist tree artifact 或文件集合。
 
-`wails_frontend_assets` 负责把该输出重根到 Go embed 可见路径，例如：
+`wails_frontend_assets` 负责把该输出重根到 Go embed 可见路径，例如：
 
 ```text
 frontend_dist/index.html
 frontend_dist/assets/index-<contenthash>.js
 frontend_dist/assets/index-<contenthash>.css
@@
 对于已有项目，`wails_app` 不强制移动 Go 文件目录；它应能接入项目已有 assets 包或由项目显式提供 asset library。
+
+重要约束：`//go:embed` 只能嵌入声明该 directive 的 Go package 逻辑目录下的文件。因此 frontend assets 必须进入包含 `//go:embed` 的 `go_library.embedsrcs`。不能把 frontend dist 直接塞进 `go_binary.srcs`，因为 rules_go 的 `go_binary.srcs` 只允许 `.go`、`.s`、`.syso` 等源码/对象输入。
+
+对 `projects/game/windows_agent` 当前实现，应明确一条权威资源路径：
+
+```text
+//projects/game/windows_agent/frontend:dist
+  -> //projects/game/windows_agent/assets:frontend_dist
+  -> //projects/game/windows_agent/assets:assets
+  -> //projects/game/windows_agent/cmd/windows_agent:windows_agent_lib
+  -> //projects/game/windows_agent:windows_agent_app_binary
+```
+
+如果 `wails_app(frontend = ...)` 额外生成 `:windows_agent_app_frontend_assets`，但该输出没有被 `windows_agent_lib` 的 assets package embed，则它只是一份重复/旁路产物，不应在设计中声称该 target 管理了最终 binary 的 frontend embed。
@@
 ### Windows C++ toolchain 处理
 
-当前 Wails 目标平台仅 Windows，且 Wails v2 Windows runtime 不需要 CGo。默认 Windows binary 应使用 `pure = "on"`。
+当前 Wails 目标平台仅 Windows，且 Wails v2 Windows runtime 不需要 CGo。默认 Windows binary 应使用 `pure = "on"`。但必须把 Bazel/rules_go Windows target launcher 的 C++ toolchain 需求作为独立前置项处理。
 
 如果遇到 C++ toolchain 相关错误，需要先区分：
 
-1. **Host C++ toolchain 缺失**：例如 Bazel/JS launcher 需要本机 Linux C++ toolchain，应通过 `rules_cc` 本机 auto configure 解决。
-2. **Windows target C++ toolchain 缺失**：只有项目自身或依赖确实需要 Windows CGo/C/C++ 时才需要 MinGW/Zig CC 等 cross toolchain。本方案默认不引入。
+1. **Host C++ toolchain 缺失**：例如 Bazel/JS launcher 需要本机 Linux C++ toolchain，应通过 `rules_cc` 本机 auto configure 解决。
+2. **Windows target launcher C++ toolchain 缺失**：例如 `@@bazel_tools//src/tools/launcher:launcher` 在 target platform `@@rules_go+//go/toolchain:windows_amd64` 下解析 `@@bazel_tools//tools/cpp:toolchain_type` 失败。即使 Go binary 是 pure Go，也必须先提供匹配 Windows amd64 target platform 的 C++ toolchain 或配置 rules_go/Bazel 使用不需要该 C++ toolchain 的 launcher 策略。
+3. **Windows CGo/C/C++ 编译 toolchain 缺失**：只有项目自身或依赖确实需要 Windows CGo/C/C++ 时才需要完整 MinGW/LLVM-mingw/Zig CC 等 cross toolchain，并应显式关闭 pure mode。
+
+当前 `projects/game/windows_agent` 实测失败属于第 2 类，而不是 Wails runtime CGo 问题。迁移计划必须把该问题列为 P0。
@@
 ## 迁移步骤
 
-### Step 1：验证外部 Wails Go module 可用性
+### Step 0：修复并验证 Bazel Windows target toolchain 基线
+
+在迁移 Wails 抽象前，必须先让仓库能分析并构建最小 Windows `go_binary`。该步骤是 `projects/game/windows_agent` 当前无法构建的 P0 根因修复。
+
+1. 在 `MODULE.bazel` / `.bazelrc` / 平台定义中补齐 Bazel 9 + rules_go 所需的 C++ rules/toolchain 基线。
+2. 明确是否采用 MinGW、LLVM-mingw、Zig cc，或 Bazel/rules_go 可支持的 no-launcher / prebuilt launcher 策略。
+3. 验证 Linux host C++ toolchain 与 Windows target C++ toolchain 是两件事：host `@@platforms//host:host` 能通过不代表 `@@rules_go+//go/toolchain:windows_amd64` 能通过。
+4. 先验证最小目标：
+
+```bash
+bazel build //projects/game/windows_agent/cmd/windows_agent:windows_agent_windows
+bazel build //projects/game/windows_agent:windows_agent_app_binary
+```
+
+5. 若仍失败，必须用以下命令确认失败是否仍在 launcher/toolchain resolution：
+
+```bash
+bazel build \
+  --toolchain_resolution_debug='@@bazel_tools//tools/cpp:toolchain_type' \
+  //projects/game/windows_agent:windows_agent_app_binary
+```
+
+只有 Step 0 通过后，才能把后续 frontend、Wails deps、zip package 失败归因到对应层。
+
+### Step 1：验证外部 Wails Go module 可用性
 
 1. 恢复或确认 `go.mod` 中 `github.com/wailsapp/wails/v2` 版本。
 2. 确认 `go_deps.use_repo` 中包含 `com_github_wailsapp_wails_v2`。
 3. 暂时绕过本地 `//third_party/github.com/wailsapp/wails/v2` label，验证外部 repo label 可被 rules_go 构建。
@@
 ### Step 2：实现 `wails_go_binary`
 
 1. 新增 `tools/wails/private/go_binary.bzl`。
 2. 封装 Windows production tags/linkopts/pure/goos/goarch/out。
 3. 支持 optional `.syso` 输入。
-4. 增加 fixture 测试，验证 `DefaultInfo.files` 包含 `.exe`。
+4. 增加 fixture 测试，验证 `DefaultInfo.files` 包含 `.exe`。
+5. 默认 tags 使用 `desktop`、`production`、`wv2runtime.<value>`；不要默认添加 `native_webview2loader`。如需 legacy native loader，应设计显式属性并单测覆盖。
+6. 明确 `wails_go_binary` 是 macro，可以调用 `go_binary(...)`；如果需要 provider，必须由后续 aggregator rule 返回，不能在 rule implementation 内部实例化 `go_binary`。
@@
 ### Step 3：升级 `wails_app`
 
 1. `wails_app` 接收 `go_library`、`binary_name`、`platform`、`frontend`、`icon`、`bindings` 等属性。
 2. 内部调用 config/frontend/resources/binary 阶段。
 3. `WailsAppInfo.binary` 设置为最终 exe。
 4. `DefaultInfo.files` 默认导出最终 exe。
+5. 对已有独立 assets package 的项目，`wails_app` 必须显式区分：
+   * `frontend` 仅作为 provider/diagnostic 阶段输出；或
+   * `asset_library` / `frontend_assets` 真实接入 `go_library.embedsrcs`。
+
+   不允许文档声称 `wails_app` 管理 frontend embed，但实际 binary 仍通过项目侧 `assets:frontend_dist` 嵌入。
@@
 ### Step 4：迁移 `projects/game/windows_agent`
 
-1. 顶层 `wails_app` 增加 `go_library`、`binary_name`、`platform`。
-2. `windows_agent_win_zip.binary` 改为 `:windows_agent_app`。
-3. `cmd/windows_agent:windows_agent_windows` 改为 manual/debug 兼容 target 或删除。
-4. 维持 `assets` 包和 frontend Bazel build 不变，降低业务代码改动。
+1. 确认 `windows_agent_win_zip.binary` 指向 `:windows_agent_app`。当前仓库已经满足该点，因此不应再把 legacy binary 作为当前失败根因。
+2. 顶层 `wails_app` 保持 `go_library`、`binary_name`、`platform`。
+3. `cmd/windows_agent:windows_agent_windows` 可保留为 manual/debug 兼容 target，但它也必须能通过 Step 0 的 Windows toolchain baseline 验证。
+4. 维持 `assets` 包和 frontend Bazel build 不变，降低业务代码改动；同时移除或重新定义 `wails_app` 内部重复 frontend assets 阶段，避免形成旁路输出。
+5. 按顺序验证：
+
+```bash
+bazel build //projects/game/windows_agent/cmd/windows_agent:windows_agent_windows
+bazel build //projects/game/windows_agent:windows_agent_app_binary
+bazel build //projects/game/windows_agent/helper/input:input_helper
+bazel build //projects/game/windows_agent:windows_agent_win_zip
+```
@@
 ## 验收标准
 
+### Bazel / rules_go Windows baseline 验收
+
+* `bazel build //projects/game/windows_agent/cmd/windows_agent:windows_agent_windows` 成功。
+* `bazel build //projects/game/windows_agent:windows_agent_app_binary` 成功。
+* 构建不再失败于 `@@bazel_tools//src/tools/launcher:launcher` 的 `@@bazel_tools//tools/cpp:toolchain_type` 解析。
+* `--toolchain_resolution_debug='@@bazel_tools//tools/cpp:toolchain_type'` 显示 Windows target platform 能选中匹配的 toolchain，或明确证明已采用不需要该 toolchain 的 launcher 策略。
+
 ### 通用工具验收
 
 * `bazel test //tools/wails/...` 成功。
 * `wails_version_test` 使用 Bazel-managed Wails CLI，不依赖本机 PATH。
 * `wails_app` fixture 能导出 `WailsAppInfo.binary`。
@@
 ### Windows agent 验收
 
 * `bazel test //projects/game/windows_agent/...` 成功。
 * `bazel build //projects/game/windows_agent:windows_agent_win_zip` 成功。
 * `bazel query 'somepath(//projects/game/windows_agent:windows_agent_win_zip, //projects/game/windows_agent:windows_agent_app)'` 有路径。
+* `bazel query 'somepath(//projects/game/windows_agent:windows_agent_app_binary, //projects/game/windows_agent/assets:frontend_dist)'` 有路径，证明 frontend assets 进入真实 Go embed 闭环。
 * zip 内容仍包含：
 
 ```text
 windows-agent.exe
@@
 ### 风险 5：误把 host C++ toolchain 与 Windows C++ cross toolchain 混淆
 
-规避：默认 Windows Wails binary 使用 `pure = "on"`。只有项目出现真实 Windows CGo/C/C++ 需求时才引入 cross toolchain。
+规避：默认 Windows Wails binary 使用 `pure = "on"`，但仍要单独验证 Bazel Windows target launcher/toolchain baseline。只有项目出现真实 Windows CGo/C/C++ 需求时才把该 baseline 升级为完整 CGo cross toolchain。
+
+### 风险 6：`native_webview2loader` tag 语义过期或误用
+
+规避：默认不添加 `native_webview2loader`。如需启用 legacy native loader，必须显式配置，并在 Windows smoke test 中验证 WebView2 loader 行为。
+
+### 风险 7：`wails_app` frontend 阶段与真实 embed 路径脱节
+
+规避：对已有 assets package 的项目，设计中必须把 `assets:frontend_dist` 作为权威 embed 路径；如果 `wails_app` 生成 frontend assets，则必须证明该输出被 `go_library.embedsrcs` 消费，否则应删除该重复阶段或降级为 diagnostic/provider 输出。
