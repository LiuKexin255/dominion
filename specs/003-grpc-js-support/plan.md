# Implementation Plan: gRPC-JS Build Support & Example Service

**Branch**: `003-grpc-js-support` | **Date**: 2026-06-03 | **Spec**: [spec.md](spec.md)

**Input**: Feature specification from `/specs/003-grpc-js-support/spec.md`

**Note**: This plan follows the user's guidance to reference official grpc-node documentation (https://github.com/grpc/grpc-node) and use Bazel for all compilation, with custom rules as needed.

## Summary

Add protobuf-to-TypeScript type generation support to the Bazel build system using the officially recommended `@grpc/grpc-js` + `@grpc/proto-loader` dynamic-loading stack. Create a custom `ts_proto_library` Bazel rule under `tools/dev/js/` that wraps a repository-global `proto-loader-gen-types` executable target to generate TypeScript type files from `.proto` files for compile-time checking only. Implement an example gRPC service in TypeScript under `experimental/ts/grpc_hello_world/` that demonstrates the full pipeline: proto definition → type generation → TypeScript compilation → runtime `.proto` loading → reusable `artifact_image` Node/grpc-js packaging → runnable gRPC server verified through a repository testplan with a grpc-gateway HTTP adapter. Static JavaScript or TypeScript protobuf/gRPC stub generation remains out of scope.

## Technical Context

**Language/Version**: TypeScript 6.x (from root package.json), ES2020 target, CommonJS modules

**Primary Dependencies**:
- `@grpc/grpc-js` ^1.14.0 — pure JavaScript gRPC runtime (official, replaces deprecated native `grpc`)
- `@grpc/proto-loader` ^0.8.0 — proto file loading + `proto-loader-gen-types` CLI for TS type generation
- `@aspect/rules_ts` 3.8.8 — `ts_project` rule (already in MODULE.bazel)
- `@aspect/rules_js` 3.0.3 — `js_binary`, `js_library` rules (already in MODULE.bazel)
- `@aspect/rules_swc` 2.7.1 — SWC transpiler integration (already in MODULE.bazel)
- `rules_proto` 7.1.0 — `proto_library` rule (already in MODULE.bazel)
- `protobuf` 33.4 — protoc compiler (already in MODULE.bazel)

**Storage**: N/A (example service is stateless)

**Testing**: Bazel `ts_project` for compilation; repository `testplan`/guitar acceptance test that deploys the grpc-js service plus a Go grpc-gateway adapter and verifies runtime behavior through the exposed HTTP endpoint; `bazel build //...` and `bazel test //...` for repository-wide validation

**Target Platform**: Node.js 24.14.0 (Linux, configured via MODULE.bazel)

**Project Type**: Build system extension + example service

**Performance Goals**: Example service responds to RPC within 5 seconds of launch (SC-002)

**Constraints**: No generated proto/gRPC files committed; no static JS/TS protobuf or gRPC stubs generated; all dependencies via catalog; Gazelle doesn't generate TS BUILD files (manual); CommonJS output required for Node.js gRPC interop; acceptance must not start the service process inside a unit test; testplan acceptance uses a suite-owned grpc-gateway HTTP adapter because deploy exposes HTTP endpoints but does not expose raw gRPC endpoints; `suite.env` MUST NOT be added because the current guitar validator rejects it

**Scale/Scope**: Single custom Bazel rule + single example service with one unary RPC

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- **Authority & Style**: PASS. Plan requires implementers to read `style/api.md` (gRPC + HTTP annotation conventions), `style/golang.md` (for proto patterns and grpc-go directory structure reference), and Google TypeScript Style Guide (referenced in `style/README.md`). Example service follows `experimental/ts/hello_world` conventions.

- **Bazel Integrity**: PASS. All compilation through Bazel: proto compilation via `proto_library` + custom `ts_proto_library`, TypeScript via `ts_project` + SWC, binary via `js_binary`, Node/grpc-js image via reusable `artifact_image`. PNPM via Bazel wrapper (`bazel run @pnpm`). No direct ecosystem tool usage. Custom rule invokes the repository-global `//tools/dev/js:proto_loader_gen_types` executable through Bazel actions by default.

- **Generated Files & Dependencies**: PASS. No generated proto/gRPC files committed — all generated by Bazel actions in the output tree. `BUILD.bazel` for TypeScript projects written manually (Gazelle not configured for TS). Dependencies (`@grpc/grpc-js`, `@grpc/proto-loader`) added to `pnpm-workspace.yaml` catalog. No special exceptions needed.

- **Testing Strategy**: PASS. Plan includes a repository testplan for the TypeScript demo. Test plan: build the server target, launch the service as the system under test through the testplan/deploy flow, send an HTTP request through a Go large-test case to a deployed grpc-gateway adapter that forwards to the grpc-js service over gRPC, verify the response, and clean up. Unit tests do not start the service process in-process for acceptance. Repository-wide `bazel build //...` and `bazel test //...` validate no regressions.

- **Behavioral Acceptance**: PASS. Validation verifies: (1) `bazel build` generates TypeScript types from proto, (2) `bazel run` starts the server with runtime `.proto` loading, (3) `artifact_image` packages the Node/grpc-js service with runtime proto data and metadata, (4) the testplan launches the grpc-js service plus suite grpc-gateway adapter and a Go large-test HTTP client can receive the correct adapter response within 5 seconds, (5) full repository builds and tests pass.

- **Review Scope**: PASS. Plan includes style review (follows hello_world conventions, Google TypeScript Style) and test-code review (test matches development goal).

- **Repository Verification**: PASS. Plan includes `bazel build //...` and `bazel test //...` as final verification step.

- **Testplan Execution**: PASS. This feature adds a TypeScript demo testplan. Because deploy does not expose raw gRPC endpoints, the suite deploys a Go grpc-gateway adapter that exposes HTTP and forwards to the internal grpc-js service. The acceptance client uses existing Go `go_largetest`/`common/gopkg/testtool` HTTP infrastructure and verifies the grpc-js service through that adapter. The testplan follows the current guitar schema and intentionally omits deprecated `suite.env`.

## Project Structure

### Documentation (this feature)

```text
specs/003-grpc-js-support/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Phase 0 research findings
├── data-model.md        # Phase 1 data model
├── quickstart.md        # Phase 1 quickstart guide
├── contracts/
│   └── ts_proto_library.md  # Custom rule API contract
└── checklists/
    └── requirements.md  # Spec quality checklist
```

### Source Code (repository root)

```text
# JS/TS build tooling
tools/dev/js/
├── BUILD.bazel                        # pnpm wrapper + exports + global proto-loader-gen-types target
├── pnpm.sh                            # Existing pnpm wrapper
├── vite.bzl                           # Existing Vite rule
└── ts_proto_library.bzl              # Custom proto-loader-gen-types TypeScript type rule

# Example gRPC service
experimental/ts/grpc_hello_world/
├── BUILD.bazel                        # proto_library + ts_proto_library + ts_project + js_binary + image/deploy/test targets as needed
├── package.json                       # @dominion/grpc_hello_world, catalog deps
├── tsconfig.json                      # CommonJS, ES2020, strict, generated/ included
├── .swcrc                             # SWC config matching tsconfig
├── greeter.proto                      # Simple Greeter service definition
├── generated/                         # Bazel output import layout for generated TS types (not committed)
├── service/
│   └── service.yaml                   # grpc-js service deployment material
├── testplan/
│   ├── gateway/
│   │   ├── main.go                    # grpc-gateway HTTP -> gRPC adapter for testplan
│   │   └── service.yaml               # HTTP adapter deployment material
│   ├── BUILD.bazel                    # go_largetest target + suite adapter targets
│   ├── deploy.yaml                    # Deploys grpc-js service + suite grpc-gateway adapter
│   ├── interface_test.yaml            # Testplan definition
│   ├── interface_test.go              # Go HTTP acceptance case
│   └── gateway/
│       ├── main.go                    # grpc-gateway adapter for testplan
│       └── service.yaml               # Suite-owned HTTP adapter deployment material
└── src/
    └── server.ts                      # gRPC server implementation

# Updated existing files
pnpm-workspace.yaml                    # +2 catalog entries (@grpc/grpc-js, @grpc/proto-loader)
.bazelignore                           # +1 entry (experimental/ts/grpc_hello_world/node_modules)
```

**Structure Decision**: The custom rule lives in `tools/dev/js/` with existing JavaScript/TypeScript build tooling. `tools/dev/js:proto_loader_gen_types` provides the default repository-global executable used by `ts_proto_library`, while the rule still allows an explicit `tool` override for exceptional projects. The example service lives in `experimental/ts/grpc_hello_world/` parallel to `experimental/golang/grpc_hello_world/`. Test deployment files mirror the existing `service/service.yaml` + `testplan/deploy.yaml` layout, with an additional suite-owned grpc-gateway adapter under `testplan/gateway/` because only HTTP is exposed by deploy. This makes the Go ↔ TypeScript comparison natural while keeping the grpc-js service implementation in TypeScript.

## Complexity Tracking

> No constitution violations detected. All gates pass.

---

## Implementation Phases

### Phase A: JS Tooling — Global `proto-loader-gen-types` and `ts_proto_library`

**Goal**: Create reusable JS/TS Bazel tooling that generates TypeScript type definitions from `.proto` files without requiring every project to define its own proto-loader executable target.

**Files to create/modify**:
- `tools/dev/js/ts_proto_library.bzl` — Rule implementation
- `tools/dev/js/BUILD.bazel` — Export the rule and define global `proto_loader_gen_types`

**Rule behavior**:
1. Accepts `proto_library` target as input (reads `ProtoInfo` provider)
2. Extracts direct proto sources and transitive dependencies (for include path resolution)
3. Runs `proto-loader-gen-types` (from `@grpc/proto-loader` npm package) via Bazel executable action with `--outDir` set to the declared Bazel output directory
4. Declares `.ts` output files under a `generated/` layout matching the protobuf package hierarchy
5. Provides generated `.ts` type files as outputs consumable by `ts_project`; it does not generate static JavaScript or TypeScript protobuf/gRPC stubs

**Key implementation details**: `proto-loader-gen-types` resolves proto imports relative to the working directory. The rule must stage proto files (including transitive deps like googleapis) in a directory structure that matches the import paths. This is achieved by creating a TreeArtifact with the correct layout before invoking the tool. The generation flags and runtime `protoLoader.loadSync` options must remain identical: `longs=String`, `enums=String`, `defaults=true`, `oneofs=true`, and `keep_case=false`/omitted runtime `keepCase`; `grpcLib=@grpc/grpc-js` is passed for generation. The `tool` attribute defaults to `Label("//tools/dev/js:proto_loader_gen_types")` so normal projects do not declare `proto_loader.proto_loader_gen_types_binary` locally. If `keep_case` is ever enabled, runtime `keepCase: true` must also be enabled.

**Verification**: `bazel build //experimental/ts/grpc_hello_world:greeter_types` produces `.ts` files.

### Phase B: Example Proto and Package Setup

**Goal**: Create the example project structure with proto definition, package manifest, and build configuration.

**Files to create**:
- `experimental/ts/grpc_hello_world/greeter.proto` — Simple Greeter service
- `experimental/ts/grpc_hello_world/package.json` — Dependencies via catalog
- `experimental/ts/grpc_hello_world/tsconfig.json` — CommonJS, ES2020, strict, declaration output, with `rootDir` widened to `.` or omitted so Bazel-generated `generated/` type files compile with `src/server.ts`
- `experimental/ts/grpc_hello_world/.swcrc` — SWC config matching tsconfig
- `experimental/ts/grpc_hello_world/BUILD.bazel` — proto_library + ts_proto_library targets

**Files to modify**:
- `pnpm-workspace.yaml` — Add `@grpc/grpc-js` and `@grpc/proto-loader` to catalog
- `.bazelignore` — Add `experimental/ts/grpc_hello_world/node_modules`

**Verification**: `bazel build //experimental/ts/grpc_hello_world:greeter_types` succeeds and generates `.ts` files.

### Phase C: TypeScript Server Implementation

**Goal**: Implement the gRPC server in TypeScript using generated types.

**Files to create**:
- `experimental/ts/grpc_hello_world/src/server.ts` — Server implementation

**Server implementation pattern** (official grpc-node recommendation):
1. Load proto dynamically with `@grpc/proto-loader` from a deterministic runfiles path
2. Cast loaded package to generated `ProtoGrpcType`
3. Implement service handlers using generated handler interfaces
4. Register handlers and start server

**Runtime proto requirement**: `greeter.proto` must be included in the binary's runfiles/data. If a runtime-loaded proto imports Google API annotations or other transitive protos, those import files must also be available to `protoLoader.loadSync` through `includeDirs`. The example proto is intentionally simple; FR-003 is verified by a dedicated build-only annotation fixture.

**Update BUILD.bazel**: Add `ts_project` target (with SWC transpiler) and `js_binary` target. Generated type imports from `src/server.ts` use `../generated/...`, so `tsconfig.json` must include both `src/**/*.ts` and `generated/**/*.ts` and must not constrain `rootDir` to `src`.

**Verification**: `bazel build //experimental/ts/grpc_hello_world:server` succeeds.

### Phase D: Release Image Support and Testplan Acceptance

**Goal**: Package the grpc-js service through the repository release artifact path and write a testplan that verifies the grpc-js server through a deployed grpc-gateway HTTP adapter, because deploy exposes HTTP routes but not raw gRPC endpoints. The adapter must run as a separate service owned by the testplan suite and forward HTTP requests to the internal TypeScript gRPC service.

**Files to create**:
- `experimental/ts/grpc_hello_world/service/service.yaml` — Deployment material for the TypeScript grpc-js server
- `experimental/ts/grpc_hello_world/testplan/gateway/main.go` — test-suite Go grpc-gateway adapter that translates HTTP requests to `SayHello` gRPC calls
- `experimental/ts/grpc_hello_world/testplan/gateway/service.yaml` — test-suite HTTP adapter deployment material with an exposed `http` port
- `experimental/ts/grpc_hello_world/testplan/deploy.yaml` — Demo deployment material that deploys both grpc-js service and grpc-gateway adapter; only the adapter has the public HTTP route
- `experimental/ts/grpc_hello_world/testplan/BUILD.bazel` — Go `go_largetest` case target
- `experimental/ts/grpc_hello_world/testplan/interface_test.yaml` — Testplan definition exposing the adapter HTTP endpoint
- `experimental/ts/grpc_hello_world/testplan/interface_test.go` — Go HTTP acceptance case using `common/gopkg/testtool`
- `tools/release/defs.bzl` — Extend `artifact_image` so it can package Node/grpc-js services while preserving existing Go service defaults

**Test approach**:
1. Build the TypeScript server artifact through Bazel
2. Build the Go grpc-gateway adapter artifact through Bazel
3. Package both services with `artifact_image`; grpc-js uses Node-compatible image options while existing Go services keep default behavior
4. Launch the grpc-js service and suite adapter through the repository testplan/deploy workflow
5. Configure the adapter to reach the grpc-js service through the in-cluster service DNS/port for `service/service.yaml`
6. Resolve the public HTTP adapter endpoint from the testplan-provided environment with `common/gopkg/testtool`
7. Send an HTTP request such as `GET /experimental/ts/grpc-hello-world/say-hello?name=World` to the adapter using a 5-second client deadline; route the public HTTP endpoint through `apitest.liukexin.com` with a unique prefix per `style/large_test.md`
8. grpc-gateway sends `SayHello` to the grpc-js service over gRPC and returns `Hello World` as HTTP response
9. Let the testplan workflow clean up the service and suite adapter

**Update BUILD.bazel**: Add the targets needed for the server artifact, testplan grpc-gateway adapter image, Go proto/grpc/gateway generation, and `go_largetest` case. Do not add a unit test that starts the service process in-process.

**Verification**: The TypeScript demo testplan runs successfully through guitar/testplan and `bazel test //experimental/ts/grpc_hello_world/testplan:testplan_test --config=largetest` passes.

### Phase E: Repository Verification

**Goal**: Ensure the entire repository builds and tests pass.

**Steps**:
1. Run `bazel run //:gazelle` to update BUILD files
2. Run `bazel build //...` — all targets pass
3. Run the TypeScript demo testplan through the repository `testplan` skill/guitar workflow
4. Run `bazel test //...` — all tests pass (including the new testplan case target)
5. Verify no generated proto/type files exist in source tree

**Verification**: Gazelle, repository build, TypeScript demo testplan, repository tests, and generated-file checks all succeed with zero failures.

---

## Step-by-Step Implementation Guide

### Step 1: Add dependencies to pnpm catalog

Edit `pnpm-workspace.yaml` to add:
```yaml
"@grpc/grpc-js": "^1.14.0"
"@grpc/proto-loader": "^0.8.0"
```

Run: `bazel run @pnpm -- --dir /mnt/code/dominion install`

### Step 2: Update .bazelignore

Append: `experimental/ts/grpc_hello_world/node_modules`

### Step 3: Create custom Bazel rule

Create `tools/dev/js/ts_proto_library.bzl` implementing the rule described in `contracts/ts_proto_library.md`.

Update `tools/dev/js/BUILD.bazel` to export `ts_proto_library.bzl` and define the global `proto_loader_gen_types` target. The `ts_proto_library.tool` attribute defaults to this global target.

### Step 4: Create example project structure

Create all files in `experimental/ts/grpc_hello_world/`:
- `greeter.proto`, `package.json`, `tsconfig.json`, `.swcrc`, `BUILD.bazel`

### Step 5: Implement server and testplan

Create `src/server.ts`, `service/service.yaml`, `testplan/gateway/main.go`, `testplan/gateway/service.yaml`, `testplan/deploy.yaml`, and `testplan/` files.

Update `BUILD.bazel` files with ts_project, js_binary, grpc-js service artifact via `artifact_image`, Go grpc-gateway adapter artifact, Go proto/grpc/gateway targets, and go_largetest case targets.

### Step 6: Validate

Run the TypeScript demo testplan, then run `bazel build //...` and `bazel test //...`.

---

## Acceptance Verification

| ID | Verification | How |
|----|-------------|-----|
| FR-001 | Proto → TypeScript type files for dynamic loading | `bazel build //experimental/ts/grpc_hello_world:greeter_types` produces generated `.ts` type files only; no static JS/TS protobuf or gRPC stubs are generated |
| FR-002 | No committed generated files | `git status` shows no generated proto/gRPC files |
| FR-003 | Google API annotation imports work | Build a dedicated `ts_proto_library` fixture in `experimental/ts/grpc_hello_world/annotations_fixture.proto` that imports `google/api/annotations.proto` and depends on `@googleapis//google/api:annotations_proto` |
| FR-004 | Example service under experimental/ts/ | Directory `experimental/ts/grpc_hello_world/` exists with all required files |
| FR-005 | Proto + generated types + dynamic loading + unary RPC + server | `bazel run //experimental/ts/grpc_hello_world:run` starts server with runtime proto loading; testplan verifies the RPC via the grpc-gateway HTTP adapter |
| FR-006 | Follows hello_world conventions | Same ts_project/SWC/CommonJS/ES2020/strict/@dominion/catalog patterns |
| FR-007 | Catalog-based deps | package.json uses `"catalog:"` protocol exclusively |
| FR-008 | Full repo build + test | `bazel build //...` and `bazel test //...` pass |
| FR-009 | Testplan-based service acceptance | TypeScript demo testplan launches the grpc-js service plus suite grpc-gateway adapter; Go large-test calls adapter HTTP endpoint and adapter verifies `SayHello`; no unit test starts the service process in-process |
| SC-001 | Single bazel build generates types | `bazel build //experimental/ts/grpc_hello_world:greeter_types` |
| SC-002 | Server responds within 5 seconds | Go large-test HTTP client uses a 5-second deadline and verifies adapter response from the grpc-js service |
| SC-003 | Zero regressions | `bazel build //...` and `bazel test //...` |
| SC-004 | Zero generated files in source | `git status` clean |
