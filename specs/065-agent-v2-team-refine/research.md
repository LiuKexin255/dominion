# Research: agent-v2-team-refine

**Feature**: specs/065-agent-v2-team-refine/spec.md
**Date**: 2026-09-16
**方法**: 源码级调研（本地物化依赖 0.1.1-rc.2 + 本仓库插件/宿主/服务源码），无外部新依赖引入；排序通用化部分补充外部规范与社区实践调研（AIP-132/158/193、seek method、MongoDB keyset 实践、Firestore/Elasticsearch/Relay 先例）与 Go 语言习惯调研（Google Go Style Guide/Decisions、Go Code Review Comments、Effective Go——第三次裁定后实现形态以 Go 习惯为锚，第四次裁定在该锚点内收敛 seek 构造与游标类型，第五次裁定修正解析填补判据、游标指针化、值转换单次化与责任收敛、seek O(n) 增量构造，见 R0，引用均为完整 URL）。所有行号锚点以当前 `main` 工作树为准。

## R0. 关键机制核实（决策前提）

- **drain 的读权威是 `reconcile` 重建，不是 live 追加**：`Team.drain` 先 `reconcile(state)`——从各 sender 的 log 派生单元（`deriveUnits`）减去消费锚（`consumedAnchors`）与已弹出未落日志锚（`member.drained`）后**整体重建** `state.pending`（`common/js/dsh-plugins/team/src/team.ts:185-203,397-425`）。推论：成员消息的派生源是**成员 log**——任何成员（agent 或非 agent）只要提供词汇表兼容的 log，其产出就经同一机制进入接收方（D1 的出发点）。
- **消费闭环按 `source.messageId` 锚**：注入消息携带 `source: {kind: "team-broadcast", messageId: anchor}` 落入成员 log 的 `user/message` 事件后，`consumedAnchors` 将其闭包（`common/js/dsh-plugins/team/src/broadcast.ts:162-175`）；`drained` 集合保护"已弹出未落日志"窗口（`team.ts:194-203`）。
- **dsh session 抽象与成员 log 同构**：`dsh-session` 自述 "Event-sourced session log and **in-memory store**... Persistence is intentionally not implemented here"（README）；`session.events` 为冻结快照、事件词汇表（`assistant/message` 等）merge-extensible、`SessionEvent` 携带 `time/seq`——一个持有 `SessionEvent[]` 形态 log 的成员与 agent 成员在派生/渲染/消费闭包上完全同权。
- **不用真 dsh `Session` 承载扫雷成员 log**：`assistant/message` 要求 provider/model provenance，且本组合挂载的 `/invariant` 子路径插件检查 turn/step enclosure——系统成员的播报不是 agent-loop 执行转录。采用**结构兼容的最小自有 log**：条目按 `SessionEvent` 形态构造——`message` 经 `createAssistantMessage`（`source` 用固定合成 provider/model），`turn`/`step` 固定占位（派生/渲染不读取）；"类似 agent log"由词汇表兼容满足。
- **agent log 当前不是长期持久化**（四层核实）：组合无持久化插件行（`projects/game/agent_v2/cordis.yml` 全行清单）；框架基线 `@dominion/dsh-core` 仅 boot 胶水（`third_party/dsh/core/package.json`）；部署无 volume/PVC（`projects/game/agent_v2/service.yaml`、`projects/game/deploy.yaml`——`kind: stateful` 是 owner 亲和路由语义非存储语义）；跨重启仅 preset（Mongo）、planner 长期记忆（memory 服务）、session 元数据（session 服务）存活。→ 扫雷成员 log 与 agent 成员 log 对齐 = 进程内存、随物化清零（D9）。
- **终局交接分支结构**（`common/js/dsh-plugins/saolei-loop/src/orchestrator.ts:896-941`）：case 1 排队消化优先 → case 2 pendingReview 重试 → case 3 planning/reviewing→player → case 4 `peekGameEvent() !== reviewedGameEvent` 时 `drain(planner)` 非空才置 pendingReview 并驱动复盘，否则落入 player 续驱。**播报必须放在 case 4 内、`drain(planner)` 之前**（D2）。
- **跳局语义即 case 1 优先序 + 终局记录单槽覆盖**：排队消息先驱动 player（current=player）；player 开新局致终局时 runtime 的 `gameEvent` 被新记录覆盖（`common/js/dsh-plugins/saolei-loop/src/game/runtime.ts:302-310` 仅持 LATEST）——旧局交接永不发生 → 不播报即跳过，零补发逻辑。
- **proto 会话面零改动可行**：`TeamMessage.member` 为 string（wire 直通）；web 团队视图按 wire 角色串直接渲染（`projects/game/web/frontend/src/components/ChatView.tsx:204-210`）；成员视图注入按 `source.role` 标注（`projects/game/agent_v2/src/history.ts:404-423`）。role `saolei` 全链路透传，前端零改动（D7）。
- **memory 排序归属服务端**（2026-09-16 用户裁定）：快照的"按 `update_time` 取最近 10 条"由 List 接口的排序能力承担，客户端不得全量拉取后自排序。proto `Memory.update_time`（OUTPUT_ONLY，`projects/game/game.proto:158`）已由 `memoryToProto` 返回（`projects/game/memory/handler/handler.go:219-234`）；`ListMemoriesRequest` 需增 `order_by`（AIP-132）。
- **session 服务已含复合游标倒序分页先例**：`ListSessions` 按 `create_time DESC, session_id DESC` 排序，`ListPageCursor{CreateTime, SessionID}` 经 `EncodePageToken`/`DecodePageToken`（base64url NoPadding JSON、RFC3339Nano）编解码（`projects/game/session/domain/pagination.go`），仓储以 `$or` 续页过滤 + 启动建复合索引（`projects/game/session/runtime/mongo/repository.go:88-99,144-200`），handler 对坏 token 映射 INVALID_ARGUMENT（`projects/game/session/handler/handler.go:143-150`）。该先例是**单一排序形态的固定复合游标**（形状 `(create_time, session_id)` 写死于 codec 与仓储）；memory 通用化后，"游标 = 最终排序键的值序列"机制是它的泛化——session 先例保持只读参考，不追溯改造（单排序服务无泛化收益，见 D4）。
- **排序通用化社区调研（2026-09-16 第二次用户裁定："充分调研社区最佳实践"）**：
  - **AIP-132 Ordering**（https://google.aip.dev/132）：`order_by` 为逗号分隔字段列表，每字段可选 `" desc"` 后缀，升序通过省略后缀表达；"Redundant space characters in the syntax are insignificant"（`"foo, bar desc"` ≡ `"foo,bar desc"`）；Timestamp 按时间自然序比较。
  - **AIP-158 Pagination**（https://google.aip.dev/158）：token MUST opaque（URL-safe）；"When paginating, all other parameters provided to ListBooks must match the call that provided the page token"（不一致 SHOULD `INVALID_ARGUMENT`）；`page_size` 续页可变 MUST 尊重；无敏感数据时 MAY 以"内部 proto + base64"混淆 token。
  - **seek method / keyset pagination**（use-the-index-luke "Paging Through Results"，https://use-the-index-luke.com/sql/partial-results/fetch-next-page）："Paging requires a deterministic sort order"——功能上只要求"按时间倒序"也 MUST 给 `order by` 追加唯一列建立确定行序；行值比较 `(a, b) < (x, y)` 的标准定义（前缀全等 + 当前键按序比较）即"X sorts before Y"；不支持行值比较的库用 **OR 阶梯** `sale_date < ? OR (sale_date = ? AND sale_id < ?)` 等价展开；换浏览方向需反转全部比较与排序（`$lt`/`$gt` 随方向翻转）。Vlad Mihalcea 的 keyset 文章（https://vladmihalcea.com/sql-seek-keyset-pagination/）同型：`ORDER BY created_on DESC, id DESC` + `(created_on, id) < (...)` + 匹配复合索引。
  - **MongoDB keyset 社区实践**：mongo-keyset-pagination（https://github.com/Guy-Meridor/mongo-keyset-pagination）——"Your sort must define a total order. End it with a field that is unique per document... If the leading fields can tie and there is no unique tie-breaker, keyset pagination will silently skip or duplicate rows at page boundaries"；"sort keys must be backed by a compound index in the same order"。Brian Pfretzschner（https://brianp.de/posts/2024/mongodb-cursor-pagination-multiple-fields/）给出 Mongo 的复合游标 OR 阶梯标准形：`$or: [{a: {$gt: cur.a}}, {a: cur.a, b: {$gt: cur.b}}]` + `.sort({a:1, b:1})`。Guy Meridor 的实践文章（https://medium.com/@guymeridor1/how-keyset-pagination-made-our-most-used-api-over-100x-faster-for-large-datasets-on-mongodb-bdfaa03052f6）：游标取页末条目的排序字段值打包 base64；条件树 = 前缀相等 + 当前键方向比较（比较算子由排序方向×翻页方向决定）；limit+1 探测下一页；排序字段 MUST 含唯一字段。
  - **MongoDB 官方索引规则**（https://www.mongodb.com/docs/v7.0/tutorial/sort-results-with-indexes/）：复合索引 `{a:1, b:-1}` 只支撑同序 `{a:1,b:-1}` 与逆序 `{a:-1,b:1}` 的 sort；sort 键序必须与索引键序一致；"In `$or` queries, MongoDB may use multiple indexes to support a single sort operation"（OR 阶梯的每支可各自走索引）。ESR（https://www.mongodb.com/docs/manual/tutorial/equality-sort-range-guideline/）：等值键在前、排序键随后。
  - **工业先例**：Firestore 查询游标（https://cloud.google.com/firestore/docs/query-data/query-cursors 与 https://cloud.google.com/firestore/docs/reference/rest/v1/StructuredQuery）——`startAfter` 取 orderBy 各字段的值（"The order of the field values must match the order of the order by clauses"）；Standard edition 对 orderBy **缺省自动追加唯一键 `__name__`**（"`ORDER BY a` becomes `ORDER BY a ASC, __name__ ASC`"）——唯一键自动收尾的直接工业先例。Elasticsearch `search_after`（https://www.elastic.co/guide/en/elasticsearch/reference/8.19/paginate-search-results.html）——游标 = 上一页末条的 sort 值数组；"we recommend that you include a tiebreaker field in your sort... unique value for each document. If you don't, your paged results could miss or duplicate hits"；PIT 下自动追加 `_shard_doc` 隐式 tiebreaker。GraphQL Relay Cursor Connections（https://relay.dev/graphql/connections.htm）——cursor 为 opaque per-edge 字符串（编码该边的排序位置）、"The ordering must be consistent from page to page"。
- **Go 语言习惯调研（2026-09-16 第三次用户裁定："这一版代码很多不是 Golang 习惯……是否在 designer 调研时过度迁移了其他语言（SQL/JS 系参考）的风格"——本轮实现形态以 Go 官方习惯 + `style/golang.md` 为锚重铸）**：
  - **Google Go Style Guide**（https://google.github.io/styleguide/go/guide，normative+canonical）：Clarity/Simplicity 原则——"Does not have unnecessary levels of abstraction"；"If code turns out to be very complex when its purpose should be simple, this is often a signal to revisit the implementation to see if there is a simpler way"；Least mechanism——"Where there are several ways to express the same idea, prefer the one that uses the most standard tools"（核心语言构造优先，精巧机械仅在有理由时使用）。
  - **Google Go Style Decisions**（https://google.github.io/styleguide/go/decisions，normative）：**Pass values**——"Do not pass pointers as function arguments just to save a few bytes... Common instances of this include passing a pointer to a string (`*string`)"（`memoryCursorJSON{Field *string, Value *string}` 的直接反面锚点；本轮裁定字段一律 `string`，缺 field/value 即空串、由校验拒绝）；Nil slices——空 slice 声明 `var t []string` 优于 `[]string{}`、用 `len()` 判空不做 nil/空区分；In-band errors——查找失败用 `(value, ok)`/error 表达而非哨兵值；Interfaces——接口在消费侧定义、避免为抽象而抽象；Useful test failures——失败信息含 got/want/输入。
  - **Go Code Review Comments**（https://go.dev/wiki/CodeReviewComments）：**In-Band Errors**（"a function should return an additional value to indicate whether its other return values are valid"——`(spec, bool)`/`error)` 形态的权威依据）；**Pass Values**（同上 `*string` 反例）；**Receiver Type**（"If in doubt, use a pointer"）；**Receiver Names**（短名、一致）；Import 分组与重命名规则。
  - **Effective Go**（https://go.dev/doc/effective_go）：Introduction——"A straightforward translation of a C++ or Java program into Go is unlikely to produce a satisfactory result... it's important to understand its properties and idioms"（用户"过度迁移其他语言风格"质疑的官方锚点：SQL/JS 系参考只提供**语义**（seek method、游标位置语义），**实现形态**必须以 Go 习惯重铸）；**Multiple return values**——"This form can be used to improve on a couple of clumsy idioms in C programs: in-band error returns... and **modifying an argument passed by address**"；"A similar approach **obviates the need to pass a pointer to a return value to simulate a reference parameter**"（"输出参数当做输入参数的方法"（C++ 出参风格）与平行数组随行的直接反面锚点——多返回值是 Go 的替代机制）；Composite literals（`&T{field: value}` 构造、字段名初始化）。
  - **仓库规范 `style/golang.md`**（相对路径，全文已读）：指针节——对象语义 vs 元组语义分层判定；**容器元素强制 `[]*T`/`map[K]*T`**（基本类型容器不受限）；**函数参数与返回值优先指针**（`parseMessage(data []byte, path string) (*Message, error)` 为 good；元组语义如 `Point` 例外）；"若调用方需要快照或隔离，由函数内部显式深拷贝"；数组节——"函数返回空数组（长度为 0 数组）时返回 `nil`"、初始化除非必要不加长度参数；函数节——"对入参、结构体 Receiver 校验仅限于函数自身功能内需要……传入的参数已经在本仓库的其他包内进行校验，不要做重复校验"（分层重排的直接依据：domain 已校验的 sort/cursor，仓储不重复校验）；指针对象构造——无初始化用 `new`、带初始值用字面量；单元测试节——表驱动、given/when/then、禁止断言塞 helper、`Test_funcName` 命名。
  - **仓库内先例（只读参考）**：session 服务 `projects/game/session/domain/pagination.go` + `projects/game/session/runtime/mongo/repository.go`——`cursorJSON` 字段即普通 `string`（无指针）、仓储 `List` 的续页过滤为**字面两子句 `$or`**（`{create_time: {$lt}}`、`{create_time: 等值, session_id: {$lt}}`）、`cursor *domain.ListPageCursor` 为 nil 判首页。其可借鉴点（string 字段、nil/零值判首页、limit+1）全部吸收；固定字面两子句是通用 OR 阶梯在"服务只有一个固定双键排序"下的手写特例——memory 通用机制下 seek 由任意键数的阶梯循环统一生成（第四次裁定，见 D4），不做字面分支；与本轮裁定的差异：session 是单一排序形态的固定双键游标（typed struct 游标 `ListPageCursor{CreateTime, SessionID}` + cursorJSON 转换），memory 通用机制下游标为排序键形态无关的 struct 包装 entries（单一 wire 形态，无转换层）——session 先例保持只读参考、不追溯改造（单排序服务无泛化收益）。
- **Go 习惯补充调研（2026-09-16 第五次用户裁定：解析填补判据缺陷、游标指针、值转换单次化与责任归属、seek 增量构造——本轮修正的官方锚点）**：
  - **map 的存在性测试与迭代序**（[Go maps in action](https://go.dev/blog/maps)——"A two-value assignment tests for the existence of a key"（`_, ok := m[key]`）与 "Iteration order ... is not specified"（range 顺序随机，需要稳定序须自持有序结构）；[Effective Go — Maps](https://go.dev/doc/effective_go#maps)——comma-ok idiom）：`validated := make(map[string]*MemorySortTerm)` 的 comma-ok 存在性测试承载重复检查与 `memory_id` 存在性查找（填补判据），**map 值直接持有 term 本体**（field → `*MemorySortTerm`，唯一已验证存储——第五轮澄清：值不是 bool 方向记录，那是给 `seen` 换名）；map 迭代序随机由**排序趟再过一遍 items**（重放输入）恢复请求原序——不引入旁路派生结构。
  - **早返回与主流程形态**（[Effective Go — If](https://go.dev/doc/effective_go#if)——"when an `if` statement doesn't flow into the next statement—that is, the body ends in `break`, `continue`, `goto`, or `return`—the unnecessary `else` is omitted"、"The code reads well if the successful flow of control runs down the page, eliminating error cases as they arise"）：退化输入（空白 `order_by`）在函数顶部早返回固定默认值，正常主流程保持在顶层——不以 `if strings.TrimSpace(orderBy) != ""` 包裹全部主逻辑。
  - **指针 nil 表达"无值/可选"**（`style/golang.md` 指针节——函数参数与返回值优先指针；仓库先例 `projects/game/session/runtime/mongo/repository.go:146-148` `cursor *domain.ListPageCursor` 以 `cursor != nil` 判首页）：值传递的零值 struct 无法区分"未提供游标（首页）"与"手构空 Entries"——nil 指针是单一、显式的首页判据；第五次裁定据此将 memory 游标全链指针化。

## 决策清单

### D1: team 插件定义成员消息源接口（依赖倒置）——成员实现接口提供消息，team 不感知成员种类

- **Decision**: `TeamMemberRegistration.agent: AgentHandle` 泛化为 `source: TeamMemberSource`——team 插件拥有的成员消息源契约：
  - `id: string`（稳定成员标识：team 映射键、广播 sender 键）；
  - `events: readonly SessionEvent[]`（产出 log，共享事件词汇表，快照语义）；
  - `subscribe?(onEvent)`（可选：产出实时通知；drain 时派生是读权威，live 仅为优化与投影订阅面）；
  - `sectionTarget?`（可选：team section 注入目标；缺省 = 无 prompt 注入）；
  - `consumes?: boolean`（缺省 true；false = announce-only——不是 relay 接收方、不建 pending、不可 drain）。
  team 同时导出 `agentMemberSource(handle: AgentHandle): TeamMemberSource` 适配器（id = agent id、events = `agent.session.events`、subscribe = `agent.ctx.on("session/event")` 按成员过滤、sectionTarget = `ctx.systemPrompt.section`、consumes = true）——现有 agent 成员语义零变化。`deriveUnits`/`consumedAnchors`/`renderBroadcast`/`buildBroadcastMessage` 逻辑零改动，改读 `source.events`；`PendingUnit.senderSessionId`/`TeamBroadcastSource.senderSessionId` 语义放宽为"sender 成员 id"（agent 即 session id），类型放宽为 string。
- **Rationale**: 消息的派生源是成员 log（R0 第 1 条）——把"成员"抽象为消息源是群聊原语的本来形状（与 dsh "session = event-sourced log" 同构，R0 第 3 条）。非 agent 成员（本次扫雷系统、未来任意系统角色）作为 source 实现者接入，经**完全相同的**派生/渲染/消费闭包路径，team 无任何"系统广播"特设概念；后续新增成员种类不改 team。
- **Alternatives considered**:
  - team 插件加 `systemBroadcast` API + team 态 system 单元表：在 team 内引入第二类单元与专用广播概念，特设路径与成员产出双机制并存，架构负担留在 team。
  - orchestrator 直接把合成消息塞进驱动消息集：消费记账搬到编排层，破坏"驱动输入 = drain 结果 + 排队消息"的不变量形态，player 侧投递状态需跨阶段维护。
  - 给 saolei 建真实 Agent 成员：需 preset/model，违反 FR-001。

### D2: 播报触发点 = orchestrator `nextStep()` case 4 内、`drain(planner)` 之前；exactly-once 用记录级 guard

- **Decision**: case 4 命中未复盘终局记录时，先 `this.saolei.announce(gameStatsText(event))`（向扫雷成员 log 追加一条 `assistant/message` 形态事件），再 `drain(planner)`（派生必含该单元，排复盘输入集末位——followup 位）。guard：`statsSentFor: GameEventRecord | null` 记录级对象恒等（镜像 `reviewedGameEvent` 模式），同记录不重复 announce；pendingReview 重试（case 2）短路 case 4，无二次播报。附带行为增强：存在未复盘终局记录时 planner drain 必非空 → 复盘保证启动（原 `relays.length === 0` 落入 player 续驱的分支对该场景不再可达）。
- **Rationale**: FR-003 绑定交接路径的精确落点；跳局不补发由 case 1 优先序 + 记录单槽覆盖零特判导出（R0）；取消（paused 短路 pump）与刷新（dispose 清态）语义零改动。
- **Alternatives considered**: runtime 侧终局时直接播报——runtime 无成员 log 所有权且呈现决策下沉游戏层；team 侧按锚去重——防重责任分散且无法区分"同记录重发"与"新记录"。

### D3: 每局 per-type 操作计数在 GameRuntime 内维护，随 `GameStats` 携带

- **Decision**: `GameRuntimeService` 增 `operationsByType: Record<OperationType, number>`（init 清零、`executeOperation` kind==="ok" 时按 `op.type` 递增——与 `operationCount` 同点同口径，分项和恒等于总数）；`computeGameStats` 签名扩展接收该表，`GameStats` 增 `operationsByType` 字段，随 `GameEventRecord.stats` 终局携带。
- **Rationale**: `GameStats` 就是"每局定量统计"实体（`common/js/dsh-plugins/saolei-loop/src/game/board.ts:177-186`）；计数器模式与 `operationCount` 一致。
- **Alternatives considered**: 播报时从 `gameLog` 重放推导——口径绑死日志结构；放 `GameEventRecord` 顶层——归属不如 stats 内聚。

### D4: 记忆快照近因注入——ListMemories 通用排序机制（Go 习惯与分层重铸；2026-09-16 五次用户裁定）

- **Decision**: `ListMemoriesRequest` 的 `string order_by = 4` 按 **AIP-132 通用语法**解析（`{field} [desc]` 逗号分隔列表、空白不敏感、升序省略后缀），对**字段白名单映射表**（API 字段 → Mongo 字段，domain 包内私有单一事实源；首期 `memory_id`/`update_time`，白名单字段**任意键位合法**）校验，未知字段/非法后缀/重复字段 → INVALID_ARGUMENT。**指定 tie-breaker 收尾（第五轮终态判据）**：请求排序字段**完全不包含 `memory_id`** 时自动追加 `memory_id asc`（确定全序）；任意位置包含即不追加——唯一键参与任意键位都使排序元组逐文档唯一（全序），"`memory_id` 仅末位"约束不成立并删除；tie-breaker 是一个指定字段而非"扫描 Unique 字段"的通用规则；缺省（空白 `order_by`）**早返回固定默认值** `[]*MemorySortTerm{memory_id asc}`（每次调用新构造），主流程保持在顶层。**`ParseMemoryOrderBy` 终态形态**：两趟处理（"先校验后产出"的字面形态——第一趟纯校验+入 map、无外部可见产出）——校验趟逐项校验（语法 → 白名单 → 重复）并将构造的 `spec.term(descending)` 存入 `validated := make(map[string]*MemorySortTerm)`（map 是**唯一已验证存储**：field → term 本体，任一项失败即 `(nil, error)`——无部分产出，map 为函数内局部状态）；排序趟**再过一遍 items** 按请求原序从 map 取 term 组装切片（map 迭代序随机，原序由重放输入恢复——`items` 是输入产物非派生结构，无平行数组回归；直接取用无 spec→term 二次构造）；map 无 `memory_id` 才 append 填补（不在 items 中，置于输出末位）。map 兼三职：重复检查、`memory_id` 存在性查找（填补判据）、term 的直接存放与取用。**通用游标（struct 包装单一 wire 形态 + 指针签名 + typed 双形态）**：`type MemoryPageCursor struct { Entries []*MemoryCursorEntry \`json:"entries"\` }`、`MemoryCursorEntry{Field, Value string}`（json tag 直接标注；另携包内私有 `typed any`——encoding/json 不可见，导出 `Typed() any` 访问器），token = base64url(NoPadding) JSON 对象 `{"entries":[{field, value}, ...]}`；**跨函数边界一律 `*MemoryPageCursor`**（`EncodeMemoryPageToken(cursor *MemoryPageCursor) string` 全函数——nil 或空 Entries → 空 token；`DecodeMemoryPageToken(token, sort) (*MemoryPageCursor, error)`——成功恒非 nil）；**nil = 首页唯一判据**（handler 空 token 传 nil、仓储 `cursor != nil` 才 seek）；非 nil cursor 携非空且 typed 就绪 Entries（Decode 产物入参契约，手构空 Entries = 违约 fail-fast）；不编码方向（token = 位置）；`DecodeMemoryPageToken` 在 domain 一步完成解码 + 键匹配 + 类型校验 + **转换**（逐 entry 经 `sort[i].CursorValue` 一次完成校验与转换，typed 存入 entry——仓储 thereafter 零转换；跨序重放/坏 token/顶层非对象/entries 缺失或空 → `ErrInvalidPageToken`）；未来向 token 加参数 = struct 加字段（json tag），签名不变。**值转换责任（第五轮裁定：单一、成套、对称）**：`MemorySortTerm` 携带一对对称转换方法——`CursorValue(value string) (any, error)`（wire → typed）与 `CursorEntry(value any) *MemoryCursorEntry`（typed → entry：时间 UTC RFC3339Nano 格式化的唯一所在；kind/动态类型不匹配 panic fail-fast）；wire↔typed 往返收敛 domain 单处，仓储只搬 typed：`memoryDocument.sortValue(field) any` 返回 typed（零格式化）、seek 读 `entry.Typed()` 原样入 bson、next-token entries 经 `term.CursorEntry(doc.sortValue(term.Field))` 构造。**分层**：domain 全量校验（`ParseMemoryOrderBy` + 游标解码校验+转换）+ 对称值转换方法；handler 只做字符串透传与错误映射（空 token → nil cursor）；仓储收到已验证输入只做直译（`MemorySortTerm` 自包含键元素，`style/golang.md` 函数节不重复校验）。**seek 条件通用 OR 阶梯（无长度分支，O(n) 增量构造）**：对任意键数的最终排序键以单一循环统一生成——**等值前缀逐键演进**（每键一次等值化 O(1) 摊还），第 i 子句 = 前缀浅拷贝（独立 `bson.M`，值均为不可变标量）+ 当前键方向 `$gt`/`$lt` 比较，emit 后尾项退化为等值汇入前缀（第 i+1 子句的派生基）；总工作量 = 输出规模（每条输出恰写一次——零重复转换、零重复查表、零重复前缀重建），统一放入 `filter["$or"]`（单键 = 一个子句的 `$or`，语义等价于裸比较——不做"并入 filter"特例）；`memorySeekClauses(sort, cursor) bson.A` **无 error 返回**（构造路径无可失败转换）；入参仅 `sort` 与 `cursor`（term/entry 自包含），无 specs/sort/values 平行数组。索引义务约定不变：新增可排序字段 = 白名单一行 + accessor 一行 + 按消费方向建复合索引（seek 构造零改动）。JS `listMemories` 可选 `{orderBy, pageSize}`（单页即止/全页累积）；快照装载 `page_size=10 + order_by=update_time desc`；`renderMemorySnapshot` 纯透传；`MemoryEntry = {memory_id, content}`。写路径与快照冻结时机零改动。**wire 兼容性**：token 对外 opaque（AIP-158），内部 JSON 形态为对象 `{"entries":[...]}`（struct 包装）——全消费面只透传不解析且无跨版本持久 token 消费面，内部形态变化零破坏；`order_by` 对外语义面唯一变化 = 曾因"`memory_id` 非末位"被拒绝的键列表（如 `memory_id, update_time desc`）转为合法（第五轮修正，消费面无依赖）。**对象关系图**：契约 §1 item 9（Mermaid classDiagram + 值转换责任表——各对象的创建/持有/调用方向与 nil 语义）。契约：`specs/065-agent-v2-team-refine/contracts/memory-snapshot-recency.md`。
- **Rationale**: 排序/截取是集合的服务端职责（第一次裁定），排序机制 MUST 通用（第二次裁定），实现形态 MUST 以 Go 习惯为准（第三次裁定），seek 无长度分支 + 游标 struct 包装（第四次裁定）。第五次裁定的五点理由：①**填补判据的数学事实**——排序键全序 ⇔ 元组逐文档唯一；唯一字段（`memory_id`）出现在任意键位即保证之，"仅末位"没有语义必要性；且末位判据（`terms[len-1].Field != memory_id`）在 `memory_id` 处于中间位时会**重复追加**（真缺陷——`memory_id, update_time desc` 会被补成双 `memory_id`），配套的"非末位拒绝"是为堵该缺陷引入的过度约束；Firestore `__name__` 自动追加同样只发生在键列表**不含** `__name__` 时——"完全不含才填补"与其同型。②**Go 习惯**——退化输入早返回、主流程顶层（Effective Go If 节：成功路径沿页面下行）；comma-ok 存在性测试承载重复检查与填补判据、**map 值直接持有 term 本体**（`map[string]*MemorySortTerm`，R0 本轮调研）——一个 map 替代 `seen` + `validated []memoryOrderByTerm` 双中间结构并消除按序旁路 terms（第五轮澄清：bool 值 map 只是给 `seen` 换名，非用户要的形态）；map 迭代序随机由排序趟重放 items 恢复请求原序；"先校验后产出"的字面形态随之恢复（第一趟纯校验+入 map、无外部可见产出，失败即 `(nil, error)` 无部分产出——in-band 无部分结果，map 为局部状态不泄漏）。③**指针**——`style/golang.md` 函数签名优先指针；值传递零值 struct 无法区分"首页（未提供）"与"手构空 Entries"，nil 指针是单一显式判据（session 先例同型）。④**复杂度**——旧构造每子句从零重建前序条件且每项重新 `CursorValue`（Θ(n²) 次转换）；增量前缀派生 + typed 携生后：转换零次（decode 已完成）、前缀等值化每键一次；OR 阶梯输出本身 Θ(n²) 规模（n 子句共 n(n+1)/2 项）是输出下界，"O(n)"的准确含义 = 每键摊还 O(1) 的状态推进 + 总工作量输出敏感（每条输出恰写一次）。⑤**分层对称**——旧形态 token→typed 的方法定义在 domain 但由 repo 逐子句调用、doc→wire 格式化在 repo accessor：同一往返的成套知识 split 两层且转换被反复触发；"谁在为游标里的值负责"收敛为唯一答案——domain 的 term（kind 知识 + `CursorValue`/`CursorEntry` 对称方法）× entry（wire/typed 双形态载体），repo 只在文档 typed ↔ entry typed 间搬运。外部语义锚点保留（R0：AIP-132/158/193、use-the-index-luke seek method、MongoDB keyset 社区实践、Firestore `__name__`/ES tiebreaker/Relay 先例）；Go 形态锚点见 R0 第三、四、五轮调研条目。
- **Alternatives considered**:
  - **if 包裹主逻辑 + `seen`/`validated` 双中间结构 + 末位填补判据**（2026-09-16 第五次裁定否决，防重复踩坑记录）：`if strings.TrimSpace(orderBy) != ""` 包裹全部主逻辑——正常路径不应住在条件分支里（退化输入应早返回、主流程保持顶层，Effective Go If 节）；`seen map[string]bool` + `validated []memoryOrderByTerm` 双中间结构——`validated := make(map[string]*MemorySortTerm)`（field→term 本体）一个对象兼三职（重复检查/存在性查找/term 存放与取用），排序趟再过 items 恢复请求原序；末位判据 `terms[len(terms)-1].Field != memory_id` 在 `memory_id` 处于中间位时会**重复追加**（`memory_id, update_time desc` 被补出双 `memory_id`——真缺陷），填补判据必须为"请求排序字段完全不含 `memory_id`"（map 存在性查找）。
  - **`memory_id` 仅末位约束**（2026-09-16 第三、四次裁定采纳的位置约束，第五次裁定推翻）：含唯一键的复合键在**任意键位**已构成全序（元组逐文档唯一），末位约束没有语义必要性；它把合法输入（如 `memory_id, update_time desc`）错误拒绝，且正是它迫使填补判据退化为"末位检查"、从而埋下中间位重复追加缺陷——约束与缺陷同源，一并删除。
  - **游标值传递**（2026-09-16 第五次裁定否决）：`cursor domain.MemoryPageCursor` 值传递违反 `style/golang.md` 函数参数优先指针；且零值 struct 无法区分"首页（未提供）"与"手构空 Entries"——nil 指针是单一显式的首页判据（session 服务 `cursor *domain.ListPageCursor` 先例同型）。
  - **seek 每子句从零重建前序条件 + 每项重新转换**（2026-09-16 第五次裁定否决）：`for j := 0; j < i` 每子句重建前缀等值 + 每项重新 `term.CursorValue(...)`——Θ(n²) 次类型转换与构建；增量前缀派生（第 i 子句由第 i-1 子句派生：尾项退化为等值 + 追加新比较项）+ typed 携生（decode 一次转换）后，构造路径零转换、零重建、每条输出恰写一次；`memorySeekClauses` 随之失去 error 返回（无可失败转换）。
  - **值转换分层不对称**（2026-09-16 第五次裁定否决）：token→typed 的方法（`CursorValue`）定义在 domain 却由 repo 逐子句调用、doc→wire 格式化（`sortValue` 内 `Format(RFC3339Nano)`）在 repo——同一"游标值 string↔typed 往返"的成套知识 split 在两个分层且无人对值的全生命周期负责；终态：`MemorySortTerm` 携带对称方法对（`CursorValue` wire→typed / `CursorEntry` typed→entry），`MemoryCursorEntry` 携带 typed 双形态（私有字段 + `Typed()`），repo 的 `sortValue` 返回 typed 零格式化——往返收敛 domain 单处、repo 只搬 typed。
  - **per-order 定制方法 + 字面量等值匹配 + 分排序定制游标**（2026-09-16 第二次裁定否决，防重复踩坑记录）：为 `update_time desc` 定制 `listMemoriesByUpdateTimeDesc`、为缺省定制 `listMemoriesByMemoryIDAsc`（raw 游标），handler 以两个字面量等值匹配——每个新排序需求都要加仓储方法/分支/handler 字面量/一种新游标形态，排序知识与需求形状耦合。
  - **通用 Unique 扫描收尾**（2026-09-16 第三次裁定否决）：白名单行携带 `Unique bool`、`appendMemorySortTieBreaker` 扫描唯一字段补尾——收尾规则的语义是"一个指定的 tie-breaker 字段"，扫描式泛化为不存在的需求（第二个唯一字段）引入了规则不确定性（多唯一字段时补哪个？）；指定字段（`memory_id`）直判即可，行结构与收尾实现同步简化。
  - **双结构体游标 + 转换层**（2026-09-16 第三次裁定否决）：`MemoryPageCursor{Fields []string, Values []any}` ↔ `memoryCursorJSON{Field *string, Value *string}` 双结构体 + encode/decode 各一遍循环 + `any` 类型分支转换（`encodeMemorySortValue`/`decodeMemorySortValue`）——wire 形态本就是 `{field, value}` 字符串对，单一 `MemoryCursorEntry{Field, Value string}` 即自然对象；`*string` 用于区分"缺字段"不必要（空串本就不是合法白名单字段/合法值，校验即拒绝）；`any` typed values 只为在 codec 内转换再被仓储转回——值在使用点按 Kind 转换即可。
  - **平行数组派生参数**（2026-09-16 第三次裁定否决）：`memorySortSpec(sort)` 返回 `bson.D + []MemorySortFieldSpec`、`memorySeekFilter(specs, sort, values)` 三平行数组入参——specs 是 sort 的白名单派生物却随行传递（C++ 出参/平行数组风格，Effective Go Multiple return values 反面）；term 自包含映射（`MongoField`/kind 随 term 携行）后派生物消失，仓储直译不再查表。
  - **seek 字面两形态构造 + 两键上界 pin 守护**（2026-09-16 第三次裁定采纳的简化方向，第四次裁定否决）：单键 `{memory_id: {$gt|$lt}}` 并入 filter / 两键字面 `$or` 两子句，以"业务字段行 ≤1 ⇒ 键长 ≤2"pin 测试守护——按 sort 长度分支是为简化引入的**新定制**（第二轮"机制通用"裁定的违例），且键数本无两键上界（白名单加业务字段即 3+ 键），分支数随白名单增长不可维护；通用 OR 阶梯单一循环覆盖任意键数，单键自然退化为一个子句的 `$or`，无需任何上界不变量与 pin 守护。（第三次裁定曾以"泛化机械服务于不可达输入"否决 n 键阶梯——该论证依赖的两键上界前提被第四次裁定推翻；其平行数组载体形态维持否决，见上条。）
  - **裸 slice 游标类型 `type MemoryPageCursor []*MemoryCursorEntry`**（2026-09-16 第四次裁定否决）：裸 slice 无扩展位——未来向 token 添加参数（版本、过滤条件回显等）需改 wire 形态与全部接触点（构造、索引访问、签名）；struct 包装（`Entries []*MemoryCursorEntry` + json tag）使加参数 = 加字段、签名不变，token 内部 JSON 形态随之定为对象 `{"entries":[...]}`（opaque、全消费面透传，零破坏）。
  - **tie-breaker 填补与排序逻辑闭环在 repo 层**（2026-09-16 第三次裁定给出的备选方向，本轮论证后否决）：domain 只做语法/白名单校验、repo 负责收尾——cursor-排序匹配校验必须对照最终键（含收尾），收尾进 repo 则匹配校验随之进入仓储（违反"校验收敛 domain"）或在 domain/repo 双向重复收尾规则（两个事实源）；维持 domain 产出最终键 + repo 直译。
  - 客户端全量拉取后自排序截断（2026-09-16 第一次裁定否决——全量拉取浪费传输且排序属服务职责）。
  - 服务端专门"最近 N 条"非标准限制面（否决——AIP-158 `page_size` 组合 `order_by` 即得，不新增非标准参数）。
  - 受限枚举/bool 排序字段（否决——偏离 AIP-132 的 `string order_by` 惯例，扩展需改字段类型）。
  - 游标编码方向并拒绝方向不同的同键重放（否决——token 携带冗余状态；位置语义（Firestore `startAfter` 同型）更简：方向由请求推导，键序列不匹配已覆盖跨序重放）。
  - 追溯改造 session 服务单排序先例为通用机制（否决——单排序形态无泛化收益，YAGNI；memory 机制作为仓库内后续新服务的参考实现）。

### D5: 扫雷系统成员以常规 announce-only 成员注册（roster 自然渲染）

- **Decision**: orchestrator 物化注册时，registration.members 增第三项 `{source: saolei 成员 source, role: "saolei", summary: "扫雷系统，终局播报对局结果与操作统计"}`（saolei-loop 常量并导出）。成员 id = `${session}/saolei`（与 `${session}/player`、`${session}/planner` 命名同构；不是 dsh session，仅标识）。team section roster 三行自然渲染，无需独立的 announcer 声明面。
- **Rationale**: 成员消息源接口下"广播成员"就是成员——role+summary 即全部注册信息；提示词所有权保持在 team section（059 R2 边界）。
- **Alternatives considered**: `TeamRegistration.announcers` 独立清单——接口泛化后冗余；写进 persona 模板——用户可编辑面不承载系统角色介绍。

### D6: 宿主历史落地 = 订阅扫雷成员的 productions（与 MemberCollector 同构的投影路径）

- **Decision**: 扫雷成员 source 实现 `subscribe`；`TeamSessions.doMaterialize` 在 orchestrator 物化后经 `orchestrator.announcer` 访问器取得该成员并订阅其产出事件，逐条调用 `TeamHistory.appendAnnouncement(role, text)`（`ROLE_AGENT` + 单 text block + `appendMerge(member=role)`——merge 入列 + `team_message` 帧扇出）；退订随 entry teardown。成员视图帧不需专门通路：消费成员经既有 `appendMemberViewUser` 按 `source.kind === "team-broadcast"` 记 `user: [saolei] …` 并扇出 `member_view` 帧。
- **Rationale**: 与既有分层一致——team 拥传输（派生/渲染），宿主拥投影（订阅成员产出写历史），镜像 MemberCollector 订阅 agent `session/event` 的模式；无专用"系统广播事件"。
- **Alternatives considered**: team 服务发 `team/system-broadcast` 事件——投影触发器进 team，特设事件面；从首个成员 log 观测推导 merge 条目——消息未被消费时团队视图永不落条目。

### D7: 统计消息的 wire 形态与前端呈现

- **Decision**: HistoryMessage role = `ROLE_AGENT`；`TeamMergeEntry.member` 本为 string、`appendMerge` 参数类型放宽收 string；前端零改动（团队视图按 member 串渲染标签并按连续同 member 分组，saolei 消息自成一组；成员视图经 `member_view` 呈现注入形态）。
- **Rationale**: 复用既有渲染路径；`ROLE_AGENT` 与"非用户输入的成员侧消息"语义一致。
- **Alternatives considered**: `ROLE_USER`——与用户输入语义不符；系统气泡样式——spec 已裁定不做（普通成员消息样式）。

### D8: team section 提示词补两处表述

- **Decision**: (a) 广播格式说明段末尾追加：标签格式仅是系统呈现**他人**输出的包装，成员自身输出不需要也不应使用 `<角色-message>`/`<角色-tool-call>` 等标签自我包装；(b) roster 含 saolei 行（D5，成员注册自然导出）。措辞终态见 `contracts/team-member-source.md` §4。
- **Rationale**: FR-008；单一所有者在 team section（060 §4 wire 形态同源约定）。
- **Alternatives considered**: 各 persona 模板分别补——用户可编辑面不承载系统级约定。

### D9: 扫雷成员播报 log 内存态、随物化生命周期（2026-09-15 用户裁定）

- **Decision**: 扫雷成员 log 为进程内存态，生命周期 = team 物化（刷新 dispose 即清、重启即失）——与 agent 成员 log 的实际行为完全对齐（R0 第 5 条：agent log 本就非长期持久化）。持久化不做；成员源接口（`events` 由成员自持）已把 log 隔离在 saolei-loop 内，未来加持久化 store 不动 team。
- **Rationale**: 语义一致性（059 刷新清空短期记忆、team 进程内存态均为既有约定）；避免隐式改变刷新/重启后的重投行为（新成员 reconcile 无消费锚会全量重收历史播报，需独立产品裁定）。
- **Alternatives considered**: 进程内跨刷新保留/落盘跨重启——均超出 agent 成员行为对齐面，重投语义需另行裁定，留作后续可选演进。

## 涉更改文件清单（供 tasks 规划参考）

| 层 | 文件 | 变更 |
|---|---|---|
| team 插件 | `common/js/dsh-plugins/team/src/team.ts` | `TeamMemberSource` 接口 + `agentMemberSource` 适配器 + 注册/drain/relay/reconcile 读 source（announce-only 能力位） |
| team 插件 | `common/js/dsh-plugins/team/src/broadcast.ts` | sender 键语义放宽（成员 id，类型 string） |
| team 插件 | `common/js/dsh-plugins/team/src/section.ts` | roster（含 saolei 行）+ "仅输入侧"表述 |
| team 插件 | `common/js/dsh-plugins/team/src/index.ts` | 导出面：`TeamMemberSource` / `agentMemberSource` / 类型形状更新 |
| saolei-loop | `common/js/dsh-plugins/saolei-loop/src/announcer.ts`（新） | `SaoleiSystemMember`：内存 log + `announce()` + source 实现（subscribe/announce-only） |
| saolei-loop | `common/js/dsh-plugins/saolei-loop/src/orchestrator.ts` | case 4 announce + `statsSentFor` guard + 注册第三成员 + `announcer` 访问器 |
| saolei-loop | `common/js/dsh-plugins/saolei-loop/src/game/{runtime,board,text}.ts` | per-type 计数 + GameStats 扩展 + 消息模板 |
| saolei-loop | `common/js/dsh-plugins/saolei-loop/src/index.ts` | 导出面更新 |
| memory 服务 | `projects/game/game.proto` | `ListMemoriesRequest` 增 `string order_by = 4`（AIP-132 通用语法/白名单/指定 tie-breaker"完全不含才填补"/token 键匹配注释——`memory_id` 任意键位合法） |
| memory 服务 | `projects/game/memory/domain/sort.go`（新）+ `projects/game/memory/domain/{model,repository,errors}.go` | `MemorySortTerm{Field, MongoField, Descending}`（+包内 kind 与**对称**值转换方法 `CursorValue(value string) (any, error)` / `CursorEntry(value any) *MemoryCursorEntry`）、包内私有白名单 `[]*memorySortFieldSpec{Field, MongoField, kind}`（单一事实源，无 Unique 标志）、`ParseMemoryOrderBy`（空白早返回固定默认值；两趟——校验趟逐项校验（语法+白名单+重复）并填 `validated map[string]*MemorySortTerm`（term 唯一存放），排序趟再过 items 按原序取 term——map 完全不含 `memory_id` 才追加 tie-breaker；返回 `[]*MemorySortTerm`）；`ListMemories` 签名 `(sort []*MemorySortTerm, cursor *MemoryPageCursor, pageSize)`；`ErrInvalidPageToken` |
| memory 服务 | `projects/game/memory/domain/pagination.go` | 通用游标（struct 包装单一 wire 形态 + typed 双形态 + 指针签名）：`MemoryCursorEntry{Field, Value string}`（json tag 直接标注；+包内私有 `typed any`（json 不可见）+ 导出 `Typed() any`）+ `type MemoryPageCursor struct { Entries []*MemoryCursorEntry \`json:"entries"\` }`；`EncodeMemoryPageToken(cursor *MemoryPageCursor) string` 全函数（nil 或空 Entries → 空 token；base64url NoPadding JSON 对象 `{"entries":[...]}`）；`DecodeMemoryPageToken(token, sort) (*MemoryPageCursor, error)`（成功恒非 nil；base64/JSON/顶层非对象/entries 缺失或非数组或空/元素空串校验 + 与最终键匹配校验 + 逐 entry 经 `CursorValue` **一次完成校验与转换**（typed 存入 entry）；全部 wrap `ErrInvalidPageToken`）；nil = 首页唯一判据 |
| memory 服务 | `projects/game/memory/handler/handler.go` | `order_by` → `domain.ParseMemoryOrderBy`、token → `domain.DecodeMemoryPageToken(token, sort)`（空 token 跳过——`cursor` 保持 nil 传仓储）；解析错误与 `ErrInvalidPageToken` → INVALID_ARGUMENT（无排序知识） |
| memory 服务 | `projects/game/memory/runtime/mongo/repository.go` + `model.go` | 单路径 `ListMemories`（已验证输入直译、**零 string↔typed 转换**：`memorySortDocument(sort)` MongoField × 方向、`cursor != nil` 时 `memorySeekClauses(sort, cursor) bson.A` 通用 OR 阶梯——**O(n) 增量构造**（等值前缀逐键演进，第 i 子句 = 前缀浅拷贝 + 尾项方向比较、emit 后尾项退化为等值汇入前缀；无 error 返回），统一赋 `filter["$or"]`（单键 = 单子句，无长度分支）、limit+1、`memoryNextPageToken` entries 经 `term.CursorEntry(doc.sortValue(term.Field))` 构造 + 指针编码）；`memoryDocument.sortValue(field) any` **typed accessor**（memory_id → string、update_time → time.Time，零格式化；未知字段 panic 不变）；启动建 `{template, session_id, update_time: -1, memory_id: 1}` 复合索引（既有唯一索引不变） |
| memory-service | `common/js/dsh-plugins/memory-service/src/{client,snapshot,service}.ts` | `listMemories` 可选 `{orderBy, pageSize}`（单页即止）；`renderMemorySnapshot` 纯透传 + `SNAPSHOT_ORDER_BY`/`SNAPSHOT_ENTRY_LIMIT` 常量；`load` 单页装载；`MemoryEntry` 收缩为 `{memory_id, content}` |
| 宿主 | `projects/game/agent_v2/src/{session,history}.ts` | announcer 订阅 → `appendAnnouncement`；merge member 放宽 string |
| 测试计划 | `projects/game/testplan/memory_test.go`、`projects/game/testplan/helpers_test.go` | 有序列表端到端断言（排序/复合游标续页/非法 order_by 400）+ listMemories helper 增 orderBy 参数 |
| 测试计划 | `projects/game/testplan/agent_v2_{game,conversation}_test.go` | 播报/跳局断言 |

web 前端 / preset 模板：零改动。

## 结论

无 NEEDS CLARIFICATION 残留；三个子需求的技术路径均已收敛为单一可选方案（D1–D9），契约面见 contracts/ 三份文档。
